spring:
    application:
        name: GatewayService
    cloud:
        gateway:
            routes:
                -   id: dorm-service
                    uri: lb://dorm-service
                    predicates:
                        - Path=/api/v1/admin/**|/api/v1/dorm/**|/api/v1/building/**

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
    instance:
        preferIpAddress: true
        instanceId: ${spring.application.name}:${spring.application.instance_id:${random.uuid}}
        leaseRenewalIntervalInSeconds: 5
        leaseExpirationDurationInSeconds: 10

server:
    port: 8080

management:
    endpoints:
        web:
            exposure:
                include: "*"
    endpoint:
        health:
            show-details: always